export enum EntitySortField {
  Title = 'title',
  CreatedAt = 'createdAt',
}

export enum SortOrder {
  Ascending = 'asc',
  Descending = 'desc',
}

export enum LocalStorageKey {
  Token = 'exam-token',
  CurrentUser = 'exam-user',
}
